# Tests specifying music on a single stroke

length = "practice"
method = "Plain Bob Major"
courses = ["12345xxx"]
base_calls = "far"
base_music = "none" # Only count 87s as score

[[music]]
pattern = "*87"
stroke = "back"
weight.back = -10
