# Tests specifying music on a single stroke, and that max count works

length = "practice"
method = "Plain Bob Major"
courses = ["12345xxx"]
base_calls = "far"

base_music = "none"
[[music]]
pattern = "87"
stroke = "back"
weight.back = -1
count.max = 0 # Demand no 87s
name = "87s"
