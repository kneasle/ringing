length = "practice"
num_comps = 150 # 2^7 = 128 comps are possible, so this forces Monument to enumerate them all
base_calls = "none"
methods = [
    "Cambridge Surprise Major",
    "Yorkshire Surprise Major",
]
splice_style = "leads"
method_count = { min = 0, max = 300 }
music_file = "music-8.toml"
