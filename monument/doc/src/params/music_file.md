# `music_file`

**Defaults to not loading any external file.**

Relative path to a file containing music definitions (i.e. a single [`music` array](music.md)).

This provides an easy way to share music between multiple composition files.
